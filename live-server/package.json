{
  "name": "sui2-live-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev-backend": "nodemon --watch app.js app.js",
    "dev": "vite --host",
    "clean": "rm -rf data editor/dist",
    "build": "npm run clean && vite build"
  },
  "dependencies": {
    "express": "^4.18.1",
    "monaco-editor": "^0.34.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
